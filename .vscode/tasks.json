{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Maven Clean and JavaFX",
            "type": "shell",
            "command": "mvn clean javafx:run",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
